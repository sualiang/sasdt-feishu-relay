services:
  feishu-relay:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sasdt-feishu-relay
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - server/.env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
